import React from "react";
import { LinearGradientBackground } from "../Utils/LinearGradientBackground";
import { View } from "react-native";

export const FamilyOverview = ({ navigation, route }) => {
  return (
    <View>
      <LinearGradientBackground>
        <></>
      </LinearGradientBackground>
    </View>
  );
};
